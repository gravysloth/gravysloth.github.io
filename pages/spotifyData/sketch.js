
let marqueeData = {}
let streamingData = {}

function preload() {
}

function setup() {
  var canvas = createCanvas(600, 600)
  canvas.parent("sketch")
  frameRate(30)
  noStroke()
}

function draw() {
  background(0)
  if (isDataDone) {
  }
}